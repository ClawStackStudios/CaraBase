A true BaaS application trying to be as aligned with SupaBase as possible. 

I would like to be able to add project in the same way to CaraBase as i would i SupaBase. 
Public/Private API Keys, RLS for public Keys, able to have storage in the tables for files, can get sharable links from them using the LAN ip (also allowing cloudflare tunnel access over a domain name or subdomain name, for local first access with optional public access)

Thoughtful security, around meaningful areas. 

as close to supabase alignment as we can get. 